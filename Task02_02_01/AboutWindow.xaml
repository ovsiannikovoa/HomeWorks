<Window x:Class="Task02_02_01.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task02_02_01"
        mc:Ignorable="d"
        Title="О программе" Height="400" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,10"
                   Text="Простой текстовый редактор">
        </TextBlock>

        <StackPanel Grid.Row="1" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">
            <Image Source="Data/author.JPG" 
                   Width="100" 
                   Height="100" 
                   Margin="0,10"/>
            <TextBlock Text="Автор: Овсянников Олег Алексеевич" 
                       Margin="0,5"/>
            <TextBlock Text="Версия: 1.0" 
                       Margin="0,5"/>
            <TextBlock Text="Дата создания: 01.07.2025" 
                       Margin="0,5"/>

            <Button Grid.Row="2" 
                    Content="Закрыть" 
                    Width="80" 
                    HorizontalAlignment="Center" 
                    Margin="0,10" 
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
