<Window x:Class="Task02_02_01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task02_02_01"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button Content="Открыть" Click="Open_Click" Margin="10" Padding="10"/>
            <Button Content="Сохранить как" Click="SaveAs_Click" Margin="10" Padding="10"/>
            <Button Content="О программе" Click="About_Click" Margin="10" Padding="10"/>
        </StackPanel>
        
        <TextBox x:Name="MainTextBox" Grid.Row="1"
                 TextWrapping="Wrap"
                 Padding="5" Margin="3" 
                 VerticalScrollBarVisibility="Auto"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 ScrollViewer.HorizontalScrollBarVisibility="Visible"
                 ScrollViewer.VerticalScrollBarVisibility="Visible"/>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" 
                            Text="Готов"/>
            </StatusBarItem>
        </StatusBar>
                   


    </Grid>
</Window>
