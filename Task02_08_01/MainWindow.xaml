<Window x:Class="Task02_08_01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task02_08_01"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <Window.Resources>
        <DataTemplate x:Key="ProductTemplate">
            <Border x:Name="border" BorderThickness="1" BorderBrush="Gray" 
                    CornerRadius="5" Padding="10" Margin="5" Background="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding ImagePath}" Width="50" Height="50"/>
                    <StackPanel Grid.Column="1" Margin="10,0">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                        <TextBlock>
                            <TextBlock.Text>
                                <Binding Path="Price" StringFormat="{}{0:N2} ₽"/>
                            </TextBlock.Text>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Category}" Value="{x:Static local:Category.Food}">
                    <Setter TargetName="border" Property="BorderBrush" Value="Green"/>
                    <Setter TargetName="border" Property="Background" Value="#E8F5E9"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Category}" Value="{x:Static local:Category.Appliances}">
                    <Setter TargetName="border" Property="BorderBrush" Value="Blue"/>
                    <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>

    <ListBox Name="lstBox"
             ItemsSource="{Binding Products}" 
             ItemTemplate="{StaticResource ProductTemplate}"/>
</Window>
